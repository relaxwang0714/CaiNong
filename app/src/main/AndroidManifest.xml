<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.android.cainong.cainong">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Sign"></activity>
        <activity android:name=".MainContain"></activity>
        <activity android:name=".MainCustomChat"></activity>
        <activity android:name=".InCome"></activity>
        <activity android:name=".Goods"></activity>
        <activity android:name=".OrderContain"></activity>
        <activity android:name=".Count"></activity>
    </application>

</manifest>